#!/usr/bin/env node

const program = require('commander');

program
  .option('-m, --message', '< commit message >')
  .parse(process.argv);

console.log(JSON.stringify(program, null, 2)); // eslint-disable-line

return 1;
